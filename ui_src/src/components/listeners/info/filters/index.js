import { Box, Typography } from '@mui/material'
import React, { useState } from 'react'

export default function Filter(props) {

    return <Box>
        <Typography variant="subtitle1">
            {/* <strong>Filter Name : </strong> {props.listener.filter.name} */}
        </Typography>
    </Box>
}