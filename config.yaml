admin:
  port: 8081
  enabled: true
clusters:
  - name: "path-cluster"
    policy: "round_robin"
    endpoints:
      - name: "test-endpoint"
        address: "0.0.0.0"
        protocol: "http"
        port: 8082
      - name: "test-endpoint-2"
        address: "0.0.0.0"
        protocol: "http"
        port: 8083
listeners:
  name: "test-listener"
  port: 8080
  type: "https"
  tls: true
  certPath: "./certs/cert.pem"
  keyPath: "./certs/key.pem"
  listening: true
  filter:
    name: "test-filter"
    rules:
      - name: "path-rule"
        type: "path"
        value: "/"
        action: "allow"
        enabled: true
        cluster: "path-cluster"
